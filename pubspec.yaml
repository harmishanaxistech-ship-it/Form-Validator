name: smart_form_validator
description: "A production-ready Flutter package for validating forms and user input with a clean and simple API."
version: 1.0.0
homepage: https://github.com/yourusername/smart_form_validator

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
